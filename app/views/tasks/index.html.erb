<h1>ibimsderPro - Tasklist</h1>

<ul>
  <% @tasks.each do |task| %>
    <li>
      <%= link_to task.title, task_path(task) %>
      <%= task.details %>
      <%= task.completed ? "Yes" : "No" %>
    </li>
  <% end %>
</ul>

<%= link_to "add new", tasks_new_path  %>
